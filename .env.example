# Template only — copy to .env and fill in real values. .env is gitignored, never commit secrets.

# Telegram bot token from @BotFather
TELEGRAM_MATKOVSKA_BOT_TOKEN=

# Shared secret for the dashboard's admin access (any random string — generate fresh, don't reuse
# the original bot's)
DASHBOARD_ADMIN_TOKEN=

# Telegram chat_ids granted dashboard admin access (not secret, just reference)
OLEKSANDR_CHAT_ID=
MATKOVSKA_CHAT_ID=
